{
  "name": "graph",
  "version": "0.1.0",
  "description": "Analyze source file import dependencies, detect cycles, identify hot modules, and write a Mermaid dependency graph to .duoshe/CODEMAP.md",
  "enabledByDefault": false,
  "postEnableHint": "Run `duoshe graph` to analyze import dependencies.",
  "triggers": ["large codebase", "cyclic dependencies", "refactor", "architecture review"]
}
